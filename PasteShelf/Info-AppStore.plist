<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- App Store category (required for App Store submission) -->
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.utilities</string>

	<!-- Menu bar app: no Dock icon by default ("Show in Dock" flips the
	     activation policy at runtime) -->
	<key>LSUIElement</key>
	<true/>

	<!-- Only Apple-provided (exempt) encryption is used; skips the export
	     compliance questionnaire on every upload -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- URL Scheme Support -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>com.pasteshelf.PasteShelf</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>pasteshelf</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
