version: "3.8"

services:
  sync-server:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - DATABASE_HOST=postgres
      - DATABASE_PORT=5432
      - DATABASE_USERNAME=${POSTGRES_USER:-pasteshelf}
      - DATABASE_PASSWORD=${POSTGRES_PASSWORD:-pasteshelf}
      - DATABASE_NAME=${POSTGRES_DB:-pasteshelf_sync}
      - JWT_SECRET=${JWT_SECRET:-REPLACE_WITH_SECURE_PASSWORD}
      - ORG_ID=${ORG_ID:-default}
      - ENVIRONMENT=production
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  postgres:
    image: postgres:16-alpine
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-pasteshelf}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-pasteshelf}
      - POSTGRES_DB=${POSTGRES_DB:-pasteshelf_sync}
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-pasteshelf}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
    driver: local
